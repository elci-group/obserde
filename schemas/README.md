# schemas/

Reserved for a standalone, deployable schema registry format. Not yet
implemented — schemas in Phase 1 are constructed and inspected in Rust
(`obserde-schema`) or loaded from the `fixtures/` directory for testing.

Target phase: Phase 2 and later, once encoding backends make an
externally-persisted schema representation meaningful.
